[package]
name = "zos-ipc"
description = "IPC Protocol Constants for Zero OS - the single source of truth for message tags"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["rlib"]
